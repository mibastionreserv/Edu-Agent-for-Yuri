// Not used in this build. The 2D SVG presenter (avatar.js) is the shipped
// renderer; the surface is Rive-ready for a richer avatar. Kept as an empty
// stub so no build depends on a 3D engine.
export default function Avatar3D() { return null; }
